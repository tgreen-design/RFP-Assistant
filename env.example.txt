# ============================================================
#  CUSTOMINK RFP AGENT — Environment Variables
#  Copy this file to .env and fill in the values.
#  NEVER commit .env to git.
# ============================================================

# ── ANTHROPIC API KEY ────────────────────────────────────────
# Get from: https://console.anthropic.com → API Keys
ANTHROPIC_API_KEY=sk-ant-YOUR_KEY_HERE

# ── SLACK BOT TOKEN ──────────────────────────────────────────
# From: api.slack.com → Your App → OAuth & Permissions → Bot User OAuth Token
# Starts with: xoxb-
SLACK_BOT_TOKEN=xoxb-YOUR_BOT_TOKEN_HERE

# ── SLACK SIGNING SECRET ─────────────────────────────────────
# From: api.slack.com → Your App → Basic Information → App Credentials
SLACK_SIGNING_SECRET=YOUR_SIGNING_SECRET_HERE

# ── SLACK APP TOKEN (for Socket Mode) ────────────────────────
# From: api.slack.com → Your App → Socket Mode → App-Level Tokens
# Starts with: xapp-
SLACK_APP_TOKEN=xapp-YOUR_APP_TOKEN_HERE

# ── DEFAULT OUTPUT CHANNEL ───────────────────────────────────
# The Slack channel where RFP results are posted by default
# Reps can override this at submission time
RFP_DEFAULT_CHANNEL=rfp-responses
